
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<title>Art By The Pound</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<style>
body {
  margin:0;
  background:#0f0f0f;
  color:white;
  font-family:Arial, Helvetica, sans-serif;
}
header {
  padding:20px;
  text-align:center;
  font-size:32px;
  font-weight:bold;
  background:#111;
  letter-spacing:2px;
}
.toolbar {
  text-align:center;
  padding:10px;
  background:#1a1a1a;
}
button, input {
  margin:5px;
  padding:10px 16px;
  background:#222;
  color:white;
  border:1px solid #444;
}
canvas {
  display:block;
  margin:20px auto;
  border:4px solid #333;
  background:#2a2a2a;
  cursor:crosshair;
}
.gallery {
  display:flex;
  gap:12px;
  flex-wrap:wrap;
  justify-content:center;
  padding:20px;
}
.gallery img {
  width:180px;
  height:180px;
  object-fit:cover;
  border:3px solid #333;
}
footer {
  text-align:center;
  padding:20px;
  color:#777;
}
</style>
</head>
<body>

<header>ART BY THE POUND</header>

<div class="toolbar">
  <button onclick="mode='spray'">Spray</button>
  <button onclick="mode='draw'">Draw</button>
  <button onclick="mode='erase'">Erase</button>
  <input type="color" id="color" value="#ff0044">
</div>

<canvas id="canvas" width="900" height="500"></canvas>

<h2 style="text-align:center;">Gallery Preview</h2>
<div class="gallery">
  <img src="https://picsum.photos/300?1">
  <img src="https://picsum.photos/300?2">
  <img src="https://picsum.photos/300?3">
  <img src="https://picsum.photos/300?4">
</div>

<footer>
  Street Art • Digital • Canvas • Spray
</footer>

<script>
const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d');
let painting = false;
let mode = 'spray';

canvas.onmousedown = () => painting = true;
canvas.onmouseup = () => painting = false;

canvas.onmousemove = (e) => {
  if(!painting) return;
  const rect = canvas.getBoundingClientRect();
  const x = e.clientX - rect.left;
  const y = e.clientY - rect.top;
  const color = document.getElementById('color').value;

  if(mode === 'draw'){
    ctx.fillStyle = color;
    ctx.beginPath();
    ctx.arc(x, y, 4, 0, Math.PI * 2);
    ctx.fill();
  }

  if(mode === 'erase'){
    ctx.clearRect(x-10, y-10, 20, 20);
  }

  if(mode === 'spray'){
    ctx.fillStyle = color;
    for(let i=0;i<30;i++){
      ctx.fillRect(
        x + (Math.random()-0.5)*30,
        y + (Math.random()-0.5)*30,
        2,2
      );
    }
  }
};
</script>

</body>
</html>
